# Models
This is where Data Transfer Objects (DTO) and Entity models live.  

DTOs are what the Controllers will be using to define data contracts when a request is made, and what the response should look like.  

Entity models are what Data Access Objects (DAO) uses to map SQL queries.